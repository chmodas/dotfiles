# Maximum time in seconds a connection can take.
connect-timeout = 10

# Display transfer progress
progress-bar

# Maximum time in seconds an operation is allowed to take.
max-time = 90

# Show an error message if an operation fails.
show-error

# make curl attempt to figure out the timestamp of the remote file, and if that
# is available make the local file get that same timestamp
remote-time
